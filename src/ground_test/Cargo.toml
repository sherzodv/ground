[package]
name = "ground_test"
version = "0.0.0"
edition = "2021"

[dependencies]
ground_compile = { path = "../ground_compile" }
ground_be_terra = { path = "../ground_be_terra" }
ground_gen = { path = "../ground_gen" }
serde_json     = "1"
tera           = "1"
