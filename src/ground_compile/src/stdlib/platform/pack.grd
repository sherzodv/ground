use pack:std

def state {
  project = std:project
  region = [ string ]
  encrypted = boolean
} = make_state {
  kind = reference
  provider_region = string
  root = std:aws:tf:state_root
  backend = std:aws:tf:backend_s3
}
